require 'spec_helper'

describe BankPayments do
  it 'has a version number' do
    expect(BankPayments::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
